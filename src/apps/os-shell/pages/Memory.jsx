import React, {useState} from 'react';
export function Memory({artifacts,onFiles}) { const [notes,setNotes]=useState(''); return <main className="page"><h1>Memory</h1><p>Working memory: raw uploads, observed capsules, candidate addresses. Supabase-ready staging before Neo4j canon.</p><section className="drop card"><input type="file" multiple onChange={e=>onFiles([...e.target.files], notes)}/><textarea placeholder="Optional field notes: colors, shapes, highlighted associations, purpose..." value={notes} onChange={e=>setNotes(e.target.value)}/></section><div className="list">{artifacts.map(a=><article className="artifact" key={a.id}><h3>{a.name}</h3><p>{a.extracted?.summary}</p><code>{a.address?.node} · {a.address?.colorBand} · {a.address?.shapeBody}</code><pre>{JSON.stringify(a.address?.encodings || {}, null, 2).slice(0,500)}</pre></article>)}</div></main> }
