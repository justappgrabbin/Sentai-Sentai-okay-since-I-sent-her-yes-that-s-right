import React from 'react';
import { SmartBrowser } from '../browser/SmartBrowser.jsx';
export function Movement({events,onBrowserEvent,onTextIngest}) { return <main className="page"><h1>Movement</h1><p>Browser, navigation, intake motion, active session events. The browser lives here because it gets things moving.</p><SmartBrowser onBrowserEvent={onBrowserEvent}/><section className="card"><h3>Quick conversation/problem intake</h3><textarea placeholder="Talk to the OS here..." onKeyDown={e=>{if(e.key==='Enter'&&e.metaKey){onTextIngest(e.currentTarget.value); e.currentTarget.value='';}}}/><p>Press Cmd/Ctrl+Enter to ingest as a conversation field.</p></section><section className="card"><h3>Live Motion Events</h3>{events.slice(0,12).map(e=><p key={e.id}><b>{e.type}</b> <small>{new Date(e.at||e.createdAt).toLocaleTimeString()}</small></p>)}</section></main> }
