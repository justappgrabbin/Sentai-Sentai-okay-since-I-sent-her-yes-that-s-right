import React from 'react';
export function Relation({packets,relations}) { return <main className="page"><h1>Relation</h1><p>People, agents, MCP packets, collaborators, dependencies, handoffs, friend/reference graph.</p><div className="grid two"><section className="card"><h3>MCP Continuity Packets</h3>{packets.map(p=><div className="packet" key={p.id}><b>{p.event}</b><p>{p.whatChanged}</p><small>{p.urgency}</small></div>)}</section><section className="card"><h3>Relations</h3>{relations.length?relations.map(r=><p key={r.id}>{r.source} → {r.target} ({r.type})</p>):<p>No relations yet. Ingest fields to create candidates.</p>}</section></div></main> }
